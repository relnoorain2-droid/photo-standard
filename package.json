{
  "name": "photo-standard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test && node --check dist/app.js && node --check dist/itinerary.js"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
